// Application Configuration
const config = {
  api: {
    baseURL: "https://" + "api.tak8.com.au",
    emailEndpoint: "https://" + "api.tak8.com.au" + "/email/send"
  },
  contact: {
    email: "ammad" + "@" + "anata.digital"
  }
};

export default config; 